import ModeButton from "../ui/ModeButton";

export default function ModeSwitcher() {
  return (
    <div className="flex gap-2 fixed top-2 right-[10px] z-50">
      <ModeButton 
      />

    </div>
  );
}